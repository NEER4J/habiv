{
  "name": "habiv",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 127.0.0.1",
    "build": "node build.mjs",
    "test": "node --test test.mjs"
  }
}
