# Habiv browser game pack

This folder contains 50 self-contained browser games. Each numbered folder has the source HTML and metadata. Each matching ZIP contains only the uploadable `index.html`. Add the matching `thumbnail.png` separately when uploading.

All games are touch-friendly, keyboard-friendly, offline-safe, and use no external dependencies.
