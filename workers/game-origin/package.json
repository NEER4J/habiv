{
  "name": "habiv-game-origin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predeploy": "mkdir -p public/sdk && cp ../../lib/bridge/habiv-bridge.js public/sdk/habiv-bridge.js && cp ../../lib/bridge/shims/*.js public/sdk/ 2>/dev/null || true",
    "predev": "npm run predeploy",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5",
    "wrangler": "^4.131.0"
  }
}
